# Server Configuration
PORT=5173
NODE_ENV=development

# API Configuration
# Keep browser requests same-origin; the SSR server proxies this path.
VITE_API_BASE_URL=/api
API_TARGET=https://admin.vitrine.gallery

# SSR Configuration
SSR_ENABLED=true
